# How to Run

`npm run build` - builds site
`npm start` - serves site locally

# Development

`npm test` - runs build then all tests (required before pushing)
`npm run test:only` - runs tests only (assumes build already run)
`npm run lint` - runs ESLint (strict config)
`npm run lint:fix` - auto-fix lint issues where possible
